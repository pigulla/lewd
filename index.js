var L = require('./src/lewd');

//L.dict({
//    a: [
//        { x: String, y: L.some(Boolean, null, Number) }
//    ]
//})({ a: [{ x: '', y: null }]});

//L(Boolean, 'fuckers')('yo');


L.Boolean().because('it must be so')(true);
L(Boolean).because('it must be so')('true');
